# == Rust / Cargo ====================================================
# Build artifacts
/target
**/target/
*.pdb

# `Cargo.lock` — COMMIT it: this package ships a binary (`src/main.rs`),
# not just a library.
# Cargo.lock

# rust-analyzer scratch files
.idea/libraries/*.xml

# == Environment / secrets ==========================================
.env
.env.*
!.env.example
*.pem
*.key
tmp/aws-credentials

# == Logs ===========================================================
*.log
logs/
*.out

# == OS / editor =====================================================
.DS
.DS_Store
.DS_Store?
._*
.Trashes
ehthumbs.db
Thumbs.db

# Editors
.idea/
*.iml
.*code
*.swp
*.swo
*~
.nsftmp

# == gRPC codegen (regenerable) =====================================
# Generated by build.rs (tonic-build). If you prefer committing generated
# code instead of relying on `protoc`, remove the next two lines.
src/gen/
pb.rs